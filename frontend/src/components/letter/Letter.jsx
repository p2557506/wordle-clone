import React from 'react'
import "./letter.scss"

import { useContext } from 'react';
import App, { AppContext } from '../../App';

function Letter(letterPos, attemptNumber) {
  
  return (
    <div className="letter"></div>
  )
}

export default Letter